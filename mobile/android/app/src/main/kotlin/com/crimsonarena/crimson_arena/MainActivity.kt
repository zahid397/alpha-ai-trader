package com.crimsonarena.crimson_arena

import io.flutter.embedding.android.FlutterFragmentActivity

// RevenueCat's paywall (purchases_ui_flutter) is a Fragment, so the activity
// must be a FlutterFragmentActivity rather than the default FlutterActivity.
class MainActivity : FlutterFragmentActivity()
